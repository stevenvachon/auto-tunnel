{
  "name": "auto-tunnel",
  "description": "Simple HTTP(S) proxy tunnelling agents.",
  "version": "0.1.0",
  "license": "MIT",
  "author": "Steven Vachon <contact@svachon.com> (https://www.svachon.com/)",
  "repository": "stevenvachon/auto-tunnel",
  "browser": "browser.js",
  "dependencies": {
    "proxy-from-env": "^1.0.0",
    "tunnel-agent": "~0.6.0",
    "universal-url": "^1.0.0-alpha"
  },
  "devDependencies": {
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  },
  "engines": {
    "node": ">= 4"
  },
  "scripts": {
    "test": "mocha test --reporter spec --check-leaks --bail"
  },
  "files": [
    "browser.js",
    "index.js"
  ],
  "keywords": [
    "agent",
    "http",
    "https",
    "proxy"
  ]
}
